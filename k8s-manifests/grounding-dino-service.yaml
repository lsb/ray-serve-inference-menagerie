apiVersion: v1
kind: Service
metadata:
  name: grounding-dino-service
  labels:
    app: grounding-dino-service
spec:
  selector:
    app: grounding-dino-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
