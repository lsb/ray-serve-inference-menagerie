apiVersion: v1
kind: Service
metadata:
  name: gemma-service
  labels:
    app: gemma-service
spec:
  selector:
    app: gemma-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
